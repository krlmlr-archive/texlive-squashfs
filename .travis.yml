language: c

script: echo | pdflatex

before_install:
  - curl -L https://github.com/krlmlr/texlive-squashfs/raw/master/out/texlive.tar.xz | tar -Jx

install:
  - PATH=$HOME/texlive/bin/x86_64-linux:$PATH

notifications:
  email:
    on_success: change
    on_failure: always
